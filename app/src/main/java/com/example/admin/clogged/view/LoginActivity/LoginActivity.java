package com.example.admin.clogged.view.LoginActivity;

import android.support.v7.app.AppCompatActivity;
import android.os.Bundle;

import com.example.admin.clogged.R;

public class LoginActivity extends AppCompatActivity {

    //TODO Sign in the user to the database (also need sign out)
    //TODO create form for adding a user (this might require a fragment or a new activity)

    @Override
    protected void onCreate(Bundle savedInstanceState) {
        super.onCreate(savedInstanceState);
        setContentView(R.layout.activity_login);
    }
}
